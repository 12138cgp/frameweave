# 参与贡献

感谢你愿意改进 FrameWeave。提交之前请花几分钟读完这份约定。

## 提 Issue

- 先搜索已有的 Issue，避免重复
- 报告问题时请写清：版本（根目录 `VERSION`）、部署方式、复现步骤、期望结果与实际结果、相关日志
- **不要贴出**：API Key、数据库连接串、管理员口令、真实用户的数据或生成内容
- 安全漏洞请走私密渠道，见 [SECURITY.md](SECURITY.md)

## 提交代码

1. 先开一个 Issue 说明要改什么，较大的改动请先讨论方案
2. 本地开发环境的搭建见 [开发者指南](docs/开发者指南.md) 第八章「本地开发」
3. 一个 Pull Request 只做一件事，方便审阅和回滚
4. 提交前请确认：
   - 前端：`npx tsc --noEmit` 不新增类型错误，`npx next build` 能通过
   - 后端：`go vet ./...` 与 `go test ./...` 通过
   - 改了模型类型判据（`web/src/lib/model-kind-rules.ts` 或 `service/model_kind_rules.go`）时，
     两边必须一起改，并更新 `testdata/model-kind-fixture.json`，构建时会做前后端对拍
5. 与计费、点数、退款相关的改动，请先读 [开发者指南](docs/开发者指南.md) 第五章，并在 PR 里说明对扣费的影响

## 代码风格

- 沿用周围代码的写法、命名和注释密度
- 注释写「为什么这么做」以及曾经踩过的坑，而不是复述代码在做什么
- 用户可见的文案使用中文，报错要说明发生了什么、用户该怎么办

## 贡献的授权

本项目按 [PolyForm Noncommercial License 1.0.0](LICENSE) 发布。提交贡献即表示你确认：

1. 你有权提交这些内容，且其中不包含你无权授权的第三方代码或素材
2. 你的贡献按本项目的许可证发布
3. 你授予项目维护者一项永久、全球、免费、不可撤销的许可，允许其以包括商业许可在内的其他条款再授权你的贡献

如果你不同意第 3 条，请在 Pull Request 中说明，我们会另行沟通。
