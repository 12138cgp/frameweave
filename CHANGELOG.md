# 更新日志

本文件同时是构建输入：`web/next.config.ts` 会解析它并注入前端，
作为产品内「更新说明」弹窗的内容。

格式约定（改动时请保持，否则条目不会显示）：
- 版本标题：`## <版本号> - <日期>`
- 条目：`+ [类型] 正文`　—— 只有这种格式的行会进弹窗，其它行仅作为文档
- ⚠️ **每条必须写在一行内**：解析是逐行做的，换行的续行不以 `+` 开头，会被直接丢掉，
  弹窗里只显示到第一行结束为止。

## v0.1.0 - 2026-09-24

+ [说明] 首个公开版本。包含无限画布、AI 图片/视频/语音生成、素材库、提示词模板、多租户（用户／分组／项目三级隔离）与点数计量，以及管理后台的用户、分组、项目、用量分析、用户反馈、内容管理与系统设置七个模块。
+ [说明] 模型接入采用 OpenAI 兼容协议（音频生成另有「火山音频」协议）。首次部署后需在「分组管理」中配置模型渠道的 BaseURL 与 API Key，并在「系统 → 分级定价」中设置各模型单价，方可开始生成。
+ [功能] 视频生成支持文生视频、图生视频、首尾帧、参考生视频、视频编辑、视频延长六种模式，在节点上显式选择；模式按模型能力灰选，鼠标悬停显示该模式需要几个素材。
+ [功能] 音频节点支持参考生成：接一段参考音频可用相同音色念新台词，接一张图片可按图片内容生成音频；按秒计价的音频模型采用「预扣 + 按实际时长结算」。
